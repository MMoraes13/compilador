var
 cont is int;
 media, altura, soma is int;
 begin
 cont := 5;
 soma := 0;
 do

 out({Altura: });
 in (altura);
 soma := soma + altura;
 cont := cont - 1;
 while cont > 0;
 out({Media: });
 out (soma / qtd);
 end
